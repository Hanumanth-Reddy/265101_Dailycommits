# there are four types of modules in py, those you write your self, istall from exteraal
# source, preinstalled and the standard library.

# standard li contains many useful module. which include string, re, datetime, math, random,
# multiprocessing, subprocesses, socket, os, email, json, doctest, unittest,pdb, argparse and sys.

# tasks ca e done are  string parsing, data serialization, debugging, and manipulating dates, emails,
# command line arguments, and more.

#  many 3rd party py modules are stored on the Python Package Index
# we can install them using PIP, pip install module name in cli then import and use.

