# None object is used tto represent the absence of variable.
# it is similar to null in other languages.
# like other "empty' values, such as 0,[] and the empty string, it is False when converted to bool var.
print(None)

None == None

None
# when entered in console it diplays as the empty string.

def fun():
    print("hhi")

var = fun()
print(var)   # none object is returned by function that doesn't explicitly return anything else.

